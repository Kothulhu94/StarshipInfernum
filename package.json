{
  "name": "starship-infernum",
  "version": "0.1.0",
  "description": "Starship Infernum — A browser-based sci-fi survival card game powered by blackjack mechanics",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "verify:rules": "vite build --ssr src/qualityAssurance/ruleRegressionSmokeCommand.ts --outDir dist-quality --emptyOutDir && node dist-quality/ruleRegressionSmokeCommand.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
